# Lambda runtime already includes boto3/botocore, so we exclude them
# to keep the package size small

fastapi>=0.112.1
beautifulsoup4>=4.12.3
requests>=2.32.3
pydantic-settings>=2.4.0
mangum>=0.19.0
